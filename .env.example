# Global Environment Settings
GEODISPATCH_ENV=development

# Go Supervisor Configuration
SUPERVISOR_PORT=8080
SUPERVISOR_HOST=go-supervisor
AGENT_URL=http://python-agent:8000
CAMARA_URL=http://mock-camara:8090
CAMARA_API_KEY=mock-secret-key-12345

# Python Agent Configuration
AGENT_PORT=8000
# Note: Agent responds to Supervisor requests via AGENT_URL; no outbound service calls needed.

# Dashboard Configuration (SolidJS + Leaflet)
DASHBOARD_PORT=3000
SUPERVISOR_WS_URL=ws://localhost:8080/ws

# Mock CAMARA Service Configuration
MOCK_CAMARA_PORT=8090
